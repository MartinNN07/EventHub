@model List<EventHub.Web.Models.VenueViewModel>

@{
	ViewData["Title"] = "Manage Venues";
}

<div class="container py-4">
	<div class="d-flex justify-content-between align-items-center mb-4">
		<h1 class="display-5">@ViewData["Title"]</h1>
		<a asp-action="Create" class="btn btn-primary">
			<i class="bi bi-plus-circle"></i> Add Venue
		</a>
	</div>

	@if (Model.Any())
	{
		<div class="card shadow-sm">
			<div class="card-body p-0">
				<div class="table-responsive">
					<table class="table table-hover mb-0">
						<thead class="table-light">
							<tr>
								<th>Name</th>
								<th>Address</th>
								<th>Capacity</th>
								<th>Events</th>
								<th class="text-end">Actions</th>
							</tr>
						</thead>
						<tbody>
							@foreach (var venue in Model)
							{
								<tr>
									<td class="align-middle">
										<strong>@venue.Name</strong>
									</td>
									<td class="align-middle">
										<i class="bi bi-geo-alt"></i> @venue.Address
									</td>
									<td class="align-middle">
										<span class="badge bg-secondary">@venue.Capacity seats</span>
									</td>
									<td class="align-middle">
										<span class="badge bg-info">@venue.EventCount event(s)</span>
									</td>
									<td class="text-end align-middle">
										<div class="btn-group" role="group">
											<a asp-action="Edit" asp-route-id="@venue.Id" class="btn btn-sm btn-outline-primary">
												<i class="bi bi-pencil"></i> Edit
											</a>
											<a asp-action="Delete" asp-route-id="@venue.Id" class="btn btn-sm btn-outline-danger">
												<i class="bi bi-trash"></i> Delete
											</a>
										</div>
									</td>
								</tr>
							}
						</tbody>
					</table>
				</div>
			</div>
		</div>
	}
	else
	{
		<div class="alert alert-info">
			<i class="bi bi-info-circle"></i> No venues found. <a asp-action="Create" class="alert-link">Create one now</a>.
		</div>
	}
</div>
